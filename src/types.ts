type User = {
	udid: string;
	balance: string;
	bank_balance: string;
	last_work: string;
	last_crime: string;
	last_prostitution: string;
};

type items = {
	name: string;
	quantity: number;
};

export { User, items };
